# Databricks Configuration
# Required environment variables for the application

# Databricks profile name (used for audit fields: inserted_by, updated_by, etc.)
DATABRICKS_CONFIG_PROFILE="Your Profile Name"

# SQL Warehouse ID for database connections
# This is used for all CRUD operations AND for creating the api_log table
DATABRICKS_WAREHOUSE_ID="your-warehouse-id-here"

# Optional: Database logging configuration
# If not set, logging is enabled by default and uses catalog/schema from requests
DATABRICKS_LOGGING_ENABLED="true"
# DATABRICKS_LOG_CATALOG="your_catalog"  # Optional: Override catalog for api_log table
# DATABRICKS_LOG_SCHEMA="your_schema"    # Optional: Override schema for api_log table

# Note: Authentication uses Databricks SDK Config from ~/.databrickscfg
# Ensure you have configured your profile with 'databricks configure --profile "Your Profile Name"'